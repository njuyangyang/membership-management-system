<%= form_for(@point_rule) do |f| %>
  <% if @point_rule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@point_rule.errors.count, "error") %> prohibited this point_rule from being saved:</h2>

      <ul>
      <% @point_rule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :score %><br>
    <%= f.number_field :score %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
